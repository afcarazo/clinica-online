import { CardUsuarioDirective } from './card-usuario.directive';

describe('CardUsuarioDirective', () => {
  it('should create an instance', () => {
    const directive = new CardUsuarioDirective();
    expect(directive).toBeTruthy();
  });
});
