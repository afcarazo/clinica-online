import { BtnHomeDirective } from './btn-home.directive';

describe('BtnHomeDirective', () => {
  it('should create an instance', () => {
    const directive = new BtnHomeDirective();
    expect(directive).toBeTruthy();
  });
});
