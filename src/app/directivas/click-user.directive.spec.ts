import { ClickUserDirective } from './click-user.directive';

describe('ClickUserDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickUserDirective();
    expect(directive).toBeTruthy();
  });
});
